const initialState = {
	exchange: "",
	country: "",
	url: "",
	trustScore: null,
	trustRank: null,
	btcVolume: null,
	usdVolume: null
};

const reducer = (state = initialState, action) => {
	switch (action.type) {
		default:
			return state;
	}
};

export default reducer;
